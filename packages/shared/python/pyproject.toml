[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kazgaza-shared"
version = "0.1.0"
description = "Shared enums/constants for KazGaza backend and telegram-bot"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["kazgaza_shared"]
